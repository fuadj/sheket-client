<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include layout="@layout/embedded_category_tree_navigation" />

            <ListView
                android:id="@+id/item_list_list_view_items"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:verticalSpacing="5dp"/>
        </LinearLayout>
    </ScrollView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/float_btn_all_item_paste"
        android:layout_width="wrap_content"
        android:clickable="true"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:layout_margin="16dp"
        android:visibility="gone"
        android:src="@drawable/ic_action_add_light_bigger"
        app:backgroundTint="@color/branch_item_floating_btn"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/float_btn_all_item_add"
        android:layout_width="wrap_content"
        android:clickable="true"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center"
        android:layout_margin="16dp"
        android:src="@drawable/ic_action_add_light_bigger"
        app:backgroundTint="@color/branch_item_floating_btn"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/float_btn_all_item_delete"
        android:layout_width="wrap_content"
        android:clickable="true"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="16dp"
        android:visibility="gone"
        android:src="@drawable/ic_action_add_light_bigger"
        app:backgroundTint="@color/branch_item_floating_btn"/>
</android.support.design.widget.CoordinatorLayout>

