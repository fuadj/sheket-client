<?xml version="1.0" encoding="utf-8"?>
<resources>
    <integer name="invalid_user_id">-1</integer>
    <integer name="invalid_company_id">-1</integer>
    <integer name="pref_default_revision">0</integer>

    // We couldn't just start @ -1 for the "un-synced" entity ids,
    // apparently -1 is used to signal error in database inserts
    // and we might want a few "constant" ids in the database
    // (e.g: we need a root category where the "first level" categories
    // refer to) and there are a couple of these "special" entities
    // so start at -10 to give us some room and go down from there
    <integer name="default_local_entity_id">-10</integer>
</resources>